<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hospital.model.medicine.dao.MedicineDao">

    <resultMap id="medicineDtoMap" type="com.hospital.model.medicine.pojo.dto.MedicineDto">
        <id column="medicine_id" property="medicineId"/>
        <result column="medicine_name" property="medicineName"/>
        <result column="count" property="count"/>
    </resultMap>

    <update id="updateCount">
        update medicine
        set
        count = count + #{count},
        update_time = #{now}
        where id = #{id}
    </update>

    <update id="deleteCount">
        update medicine
        set
        count = count - #{count},
        update_time = #{now}
        where medicine_name = #{medicineName}
    </update>

    <select id="getMedicineList" resultMap="medicineDtoMap">
        select medicine_id,medicine_name,count
        from medicine
        where is_deleted = '0' and medicine_name like concat('%',#{medicineName},'%')
    </select>

</mapper>
